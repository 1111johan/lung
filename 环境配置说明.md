# 🔧 环境配置说明

**项目名称**: lung (GXMU TB-Agent)  
**配置时间**: 2025-01-29

---

## ✅ 环境文件已创建

`.env` 文件已成功创建，包含以下配置项：

### 必需配置

#### 1. Supabase 配置

```env
VITE_SUPABASE_URL=your_supabase_project_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
```

**获取步骤**：
1. 访问 [Supabase Dashboard](https://supabase.com/dashboard)
2. 登录或注册账号
3. 创建新项目或选择现有项目
4. 进入 **Settings** > **API**
5. 复制以下信息：
   - **Project URL** → 填入 `VITE_SUPABASE_URL`
   - **anon public** key → 填入 `VITE_SUPABASE_ANON_KEY`

**⚠️ 重要**: 这两个配置是必需的，否则应用无法启动！

### 可选配置

#### 2. 智慧生成 API 配置（DeepSeek）

```env
VITE_DEEPSEEK_API_KEY=your_deepseek_api_key_here
VITE_DEEPSEEK_BASE_URL=https://api.deepseek.com
```

**获取步骤**：
1. 访问 [DeepSeek Platform](https://platform.deepseek.com/)（智慧生成服务）
2. 注册/登录账号
3. 进入 **API Keys** 页面
4. 创建新的 API Key
5. 复制 API Key → 填入 `VITE_DEEPSEEK_API_KEY`

**注意**: 
- `VITE_DEEPSEEK_BASE_URL` 已有默认值，通常不需要修改
- 如果未配置 API Key，相关功能会显示警告但不会崩溃

---

## 📝 配置步骤

### 1. 编辑 `.env` 文件

```bash
cd /Users/a202308688/Desktop/curser/lung/lung-main
# 使用你喜欢的编辑器打开 .env 文件
nano .env
# 或
code .env
# 或
vim .env
```

### 2. 填入实际配置值

将占位符替换为实际的值：

```env
# 示例（请使用你自己的实际值）
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
VITE_DEEPSEEK_API_KEY=sk-xxxxx...
VITE_DEEPSEEK_BASE_URL=https://api.deepseek.com
```

### 3. 保存文件

保存 `.env` 文件后，重启开发服务器使配置生效。

---

## 🔒 安全提示

1. **不要提交 `.env` 到 Git**
   - `.env` 文件已添加到 `.gitignore`
   - 包含敏感信息，不应公开

2. **使用 `.env.example` 作为模板**
   - `.env.example` 可以提交到 Git
   - 包含配置项说明，不包含实际密钥

3. **保护你的 API 密钥**
   - 不要分享 `.env` 文件
   - 不要在代码中硬编码密钥
   - 定期轮换密钥

---

## ✅ 验证配置

### 检查环境变量是否加载

启动开发服务器后，检查控制台：

```bash
npm run dev
```

**正常情况**：
- 如果 Supabase 配置正确，应用会正常启动
- 如果智慧生成 API Key 未配置，会显示警告（不影响启动）

**错误情况**：
- 如果缺少 `VITE_SUPABASE_URL` 或 `VITE_SUPABASE_ANON_KEY`，应用会抛出错误：
  ```
  Error: Missing Supabase environment variables
  ```

---

## 🗄️ 数据库迁移

配置 Supabase 后，还需要执行数据库迁移：

1. 访问 Supabase Dashboard
2. 进入 **SQL Editor**
3. 打开文件：`supabase/migrations/20251229073045_create_tb_screening_system.sql`
4. 复制 SQL 内容
5. 在 SQL Editor 中执行

---

## 📋 配置检查清单

- [ ] `.env` 文件已创建
- [ ] `VITE_SUPABASE_URL` 已配置
- [ ] `VITE_SUPABASE_ANON_KEY` 已配置
- [ ] `VITE_DEEPSEEK_API_KEY` 已配置（可选）
- [ ] 数据库迁移已执行
- [ ] 开发服务器可以正常启动

---

## 🚀 下一步

配置完成后，可以启动项目：

```bash
# 1. 确保依赖已安装
npm install

# 2. 启动开发服务器
npm run dev
```

应用将在 `http://localhost:3000` 启动。

---

**配置完成时间**: 2025-01-29  
**配置文件位置**: `/Users/a202308688/Desktop/curser/lung/lung-main/.env`

